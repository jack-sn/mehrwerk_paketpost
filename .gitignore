# Betriebssystem
.DS_Store
Thumbs.db
desktop.ini

# Nextcloud-Konfliktdateien (entstehen beim Sync über mehrere Rechner)
*conflicted copy*
*Konflikt*

# Editor
.vscode/
.idea/
*.swp

# Werkzeug-Zwischenstand, gehört nicht zur Website
.impeccable/

# Figma-Token, gehoert niemals ins Repo
.figma-token
redaktion/.figma-token

# Redaktion: Arbeitsfassung der Texte, gehoert nicht auf die oeffentliche Seite
redaktion/
